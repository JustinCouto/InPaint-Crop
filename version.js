// App version - update this when releasing new versions
const APP_VERSION = 'v1.1.0';

// Export for ES modules (script.js)
if (typeof module !== 'undefined' && module.exports) {
  module.exports = { APP_VERSION };
}
